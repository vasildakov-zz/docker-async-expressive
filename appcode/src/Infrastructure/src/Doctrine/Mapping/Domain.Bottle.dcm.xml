<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping
        xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                        http://raw.github.com/doctrine/doctrine2/master/doctrine-mapping.xsd">

    <entity name="Domain\Bottle" table="bottle">

        <!-- definition here -->
        <id name="id" type="string">
            <generator strategy="UUID" />
        </id>

        <!-- properties -->
        <field name="name" column="name" type="string" nullable="false" />
        <field name="age" column="age" type="integer" nullable="true" />
        <field name="vintage" column="vintage" type="integer" nullable="true" />

        <!-- relations -->
        <many-to-one field="distillery" target-entity="Distillery" inversed-by="bottles">
            <join-column name="distillery_id" referenced-column-name="id" />
        </many-to-one>

    </entity>

</doctrine-mapping>
